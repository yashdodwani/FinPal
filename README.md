# FinPal
- Wire NeonDB connection and models.
- Integrate Gemini API client.
- Implement agents and orchestration logic.
- Configure Alembic and migrations.
## TODO

See the repository tree for packages. Developer A will implement agents under `app/agents/master`, `app/agents/loan`, `app/agents/policy`. Developer B will implement agents under `app/agents/scam`.

## Structure

3. Run the app with uvicorn.
2. Copy `.env.example` to `.env` and set values.
1. Create a virtual environment and install dependencies.

## Quick start

A multi-agent backend skeleton built with FastAPI, targeting Gemini for AI agents and NeonDB (PostgreSQL) for storage. This repository provides a minimal scaffolding with TODOs and placeholders—no business logic yet.


